import SwiftUI

struct RatingsView: View {

    var body: some View {
            VStack(spacing: 0) {
                Spacer()
            }
            .maxWidth()
            .padding(8)
            .background(Color.background)
    }

}
